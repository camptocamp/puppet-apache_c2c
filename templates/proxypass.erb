# file managed by puppet

<Proxy <%= url %>*>
    Order deny,allow
    Allow from all
</Proxy>

ProxyPass <%= location %> <%= url -%> <%= params.join(' ') %>
ProxyPassReverse <%= location %> <%= url %>

